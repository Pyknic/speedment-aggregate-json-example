# Server Settings
server.port = 9783

# Speedment Settings
speedment.username = root
speedment.password = password

# Datasource
spring.datasource.url      = jdbc:mysql://127.0.0.1:3306/employees?useSSL=false
spring.datasource.username = root
spring.datasource.password = password